__executable: "bin/elke_test"

unitTestCLI_help1:
  args: "-h --nocolor"
  checks: [{type: ExitCodeCheck}]
  requirements: ["basic1", "cli1"]

unitTestCLI_help2:
  args: "--help --nocolor"
  checks: [{type: ExitCodeCheck}]
  requirements: ["basic1", "cli1"]

unitTestCLIs.cc:
  args: "-b 'call elke::unit_tests::unitTestVec3' --nocolor"
  debug: True
  requirements: ["basic1", "cli1"]
  checks: [
    {
      type: ExitCodeCheck,
    }]